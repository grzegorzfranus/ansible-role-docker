#jinja2: trim_blocks: True, lstrip_blocks: True
{{ ansible_managed | comment }}
[Unit]
Description=Docker configuration and metadata backup
After=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/bin/docker-backup
